cmake_minimum_required(VERSION 3.20)
project(fogpass_ui LANGUAGES CXX)

# Placeholder target - to be replaced with actual Qt/QML build rules.
add_custom_target(fogpass_ui ALL
    COMMAND ${CMAKE_COMMAND} -E echo "Configure Qt/QML build here"
    COMMENT "Fogpass UI placeholder target"
)
